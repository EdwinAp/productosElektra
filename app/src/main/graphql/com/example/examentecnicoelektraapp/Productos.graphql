query Products($skus: [String]) {
    viewer {
        products(skus: $skus) {
            id
            name
            image
            price
            priceFormated
            lastPrice
            sku
            thumb
        }
    }
}